[build]
  base = "apps/frontend"
  publish = "apps/frontend/dist"
  command = "cd apps/frontend && pnpm install && pnpm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
